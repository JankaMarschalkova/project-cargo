import { Typography } from '@mui/material';

import usePageTitle from '../hooks/usePageTitle';

const PublishRide = () => {
	usePageTitle('PublishRide');
	return (
		<>
			<Typography variant="h2">PublishRide</Typography>
		</>
	);
};

export default PublishRide;
